# ═══════════════════════════════════════════════════════════════
# Caddy — production (api.codewalk.xyz)
# Copy to server: /opt/codewalk/Caddyfile
# Then: docker compose restart caddy
# ═══════════════════════════════════════════════════════════════

api.codewalk.xyz {
    reverse_proxy codewalk-api:8000
}

# ─── DEV FALLBACK: IP-only HTTP (comment out domain block above) ───
# :80 {
#     reverse_proxy codewalk-api:8000
# }
