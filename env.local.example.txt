# ═══════════════════════════════════════════════════════════════════════
#  LOCAL .env — copy to codewalk repo root: cp env.local.example.txt .env
#  Used by: local uvicorn, MCP host process, CLI (NOT the Hetzner server)
#
#  Server-only keys (POSTGRES_PASSWORD, RATE_LIMIT_*, GITHUB_APP_*, etc.)
#  are ignored locally — safe if you copied env.server.example.txt by mistake.
# ═══════════════════════════════════════════════════════════════════════

# ── LLM (required for local query / MCP) ──────────────────────────────
# Ollama (free, local):
# LLM_PROVIDER=ollama
# LLM_MODEL=qwen2.5-coder:7b

# Or cloud provider:
# LLM_PROVIDER=deepseek
# LLM_MODEL=deepseek-chat
# DEEPSEEK_API_KEY=

# ── Embeddings (local index only — cloud MCP downloads pre-built index) ─
# EMBEDDING_MODEL=jinaai/jina-code-embeddings-1.5b

# ── Cloud MCP (download index from api.codewalk.xyz) ──────────────────
# Set these in .vscode/mcp.json instead — see env.example.txt Section C
# CODEWALK_SERVER_URL=https://api.codewalk.xyz
# CODEWALK_REPO_NAME=owner/repo
# CODEWALK_REPO_TOKEN=cw_repo_xxxxxxxx
